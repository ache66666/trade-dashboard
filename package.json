{
  "name": "market-workbench",
  "version": "1.0.0",
  "private": true,
  "description": "交易员市场数据工作台",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "seed:staging:dry-run": "node scripts/seed-staging.js --dry-run",
    "seed:staging": "node scripts/seed-staging.js",
    "verify:staging-seed": "node scripts/verify-staging-seed.js",
    "seed:staging:clean": "node scripts/clean-staging-seed.js",
    "migrate:staging:journal": "node scripts/migrate-staging-journal.js"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
