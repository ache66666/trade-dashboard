{
  "name": "market-workbench",
  "version": "1.0.0",
  "private": true,
  "description": "交易员市场数据工作台",
  "scripts": {
    "start": "node server.js",
    "start:local": "node --env-file=.env server.js",
    "test": "node --test",
    "data:fred:dry-run": "node --env-file=.env.production.local scripts/run-fred-connector.js --dry-run",
    "journal:migration:production:dry-run": "node scripts/migrate-production-journal.js --dry-run",
    "journal:migration:production": "node scripts/migrate-production-journal.js"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
